name: Deploy to GitHub Pages

on:
  push:
    branches: [master]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # EPC Demo 빌드
      - name: Install & Build epc-demo
        working-directory: epc-demo
        run: |
          npm install
          npm run build

      # Samsung Life PoC Demo 빌드
      - name: Install & Build samsung-life-poc-demo
        working-directory: samsung-life-poc-demo
        run: |
          npm install
          npm run build

      # 배포용 폴더 조합: 루트 index.html + 각 데모
      - name: Assemble site
        run: |
          mkdir -p _site/epc-demo _site/manufacturing-agent-demo _site/samsung-life-poc-demo
          cp index.html _site/
          touch _site/.nojekyll
          cp -r epc-demo/dist/* _site/epc-demo/
          cp -r manufacturing-agent-demo/. _site/manufacturing-agent-demo/
          cp -r samsung-life-poc-demo/dist/* _site/samsung-life-poc-demo/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
