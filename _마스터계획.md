# Dave's Sionic AI Demo Showcase — 마스터 계획·핸드오프

> compact 후 이 문서를 먼저 읽고 작업 재개. 상세 세션 기록은 메모리 `project_demo_showcase.md` 참조.
> 최종 갱신: 2026-06-30 (#14 건축도서 도면 비교 재생 데모 추가)

---

## 1. 프로젝트 개요
- **목적**: 전시·영업 시연 때 데모를 한곳에서 쉽게 꺼내보는 쇼케이스 사이트
- **라이브**: https://dave-sionic-showcase.vercel.app (회사 Vercel "Sionic AI Engineering" 팀, CLI 계정 `dave-3661`)
- **레포**: `wonsj3712/demo-showcase` (개인 GitHub) / 로컬 `C:/Users/wonsj/Claude Code Work/demo-showcase`
- **자동배포**: GitHub master push → Vercel 자동 빌드·배포 (대시보드 연결 완료). 수동 배포 = `cd demo-showcase && vercel deploy --prod`
- 기존 `wonsj3712.github.io/demo-showcase`는 폐기(GitHub Pages 워크플로 제거)

## 2. 현재 상태 (★작업 재개 시 먼저 확인)
**전부 완성·배포·라이브 동작 확인됨.** 13개 카드, 산업별 4개 폴더 그룹핑 + 카드·내장화면 실명 익명화 완료(2026-06-24, 커밋 `22606a1`). 라이브 검증: 폴더·익명 제목 노출 + 배포물 `public/`에 실명 문자열 0건.

| # | 폴더 | 카드 제목(익명) | 경로 | 실제 고객사 | 엔진 |
|---|------|----------------|------|------------|------|
| 1 | 금융 | AI 손해보험 설계사·심사 코파일럿 | /db-copilot-demo/ | DB손해보험 | 실엔진✅ |
| 2 | 금융 | AI 생명보험 설계사·심사 코파일럿 | /nh-life-copilot-demo/ | NH농협생명 | 실엔진✅ |
| 3 | 금융 | AI 보험심사 에이전트 (지급·가입·법령) | /s-life-insurance-poc-demo/ | 삼성생명 | 실엔진✅ |
| 4 | 금융 | 해외 사모펀드 입출금 자동화 | /samsungfire-pe-poc-demo/ | 삼성화재 | mock |
| 5 | 금융 | AI 주택담보대출 간편심사 | (외부)ibk-1stlab-...vercel.app | IBK기업은행 | 외부 |
| 6 | 금융 | 책임경영 이행점검 시스템 | (외부)sionic-kodit.vercel.app | 기술보증기금 | 외부 |
| 7 | 금융 | 감정평가서 데이터화·질의응답 | (외부)kodata-appraisal-demo.vercel.app | 한국평가데이터 | 외부 |
| 8 | 금융 | 펀드 설명서 대조 검증 에이전트 | (외부)nh-fund-poc.vercel.app | NH농협은행 | 외부 |
| 9 | 금융 | 컴플리가드 · 준법감시 에이전트 | (외부)nh-compliguard-demo.vercel.app | NH농협은행 | 외부 |
| 10 | 금융 | Venture Capital Ontology | (외부)dev-samsung-vc-insight.sionic.im | 삼성VC(이미 익명) | 외부 |
| 11 | 제조 | 설비 예방보전 AI 관리 시스템 | /demo-showcase/manufacturing-agent-demo/ | DK산업(가상) | mock(정적) |
| 12 | 건설·엔지니어링 | EPC Email Issue Management | /epc-demo/ | (없음) | mock |
| 13 | 플랫폼·도구 | STORM APIs Parse Differ | (외부)storm-apis-parse-differ.vercel.app | (자사도구) | 외부 |
| 14 | 건설·엔지니어링 | 건축도서 도면 비교 검증 | /floor-plan-demo/ | (익명 샘플 ㅁㅁ) | mock(재생) |

- **내장 데모**(이 레포 빌드, 화면 내부까지 익명화됨): 1,2,3,4,11,12,14 / **외부 링크**(카드만 익명화, 외부 사이트 화면엔 실명 잔존): 5~10, 13
- **#14 건축도서 도면 비교**(2026-06-30 추가): 정석현(줄리앙) 원작 `wonsj3712/dave-agentic-parsing-floor-plan`(FastAPI+React+Gemini)을 **재생 데모**로 이식. 실제 백엔드 대신 사전 캡처한 분석 결과(`static/assets/data/answers.json`)+사전 인덱싱 도면 메타·이미지를 정적 재생. 원본 frontend 화면 그대로 재사용, `src/api.ts`만 정적 mock으로 교체. 추천질문 5종(분류·사업개요비교·84A평면도비교+diff이미지12장·마감표비교·단순사실). 샘플 도면은 회사명 `ㅁㅁ` 익명. **publicDir=`static`**(레포 `.gitignore`의 `public/` 회피)
- **실엔진 보험 데모 3종**: 1,2,3 (실제 약관 PDF RAG)
- 각 카드에 "에이전트" 칩(STORM answer 링크, 시연자 로그인용)
- **익명화 방식**: 카드 제목엔 회사명 0, 설명에만 산업 범주(대형 손해보험사·대형 생명보험사·국책은행·기술보증기관·기업데이터 평가기관·대형 은행 준법감시부). 내장 화면: db/nh `company`→'손해보험'/'생명보험'+상품명·페르소나·탭제목 익명 / samsungfire 삼성화재→당사, Samsung Fire & Marine→Korea P&C Insurance / s-life "S Life"→생명보험. **코드 주석의 실명은 프로덕션 빌드 시 제거**(배포물 0건). 단 "실손·암·운전자" 등 디테일로 회사 유추는 가능(명시 실명만 제거)
- ⚠️ **미진행**: 외부 데모(5~10,13) 화면 내부 익명화(별도 레포) / **lg-superstart-demo/**(제조 추가 데모, 사용자 마무리 중 — build.mjs·카드 미포함)

## 3. 실엔진 데모 — STORM 에이전트 매핑 (sample@sionicstorm.ai, 팀 Sionic AI Testbed `d1de06c56e8046e2a2f86080`)
| 데모 | 모드/role | agentId | 지식(버킷) |
|------|----------|---------|-----------|
| s-life | payment(지급) | 7472850492290461696 | 실제 보험약관·상법 보험편 리서치본 |
| s-life | underwriting(가입) | 7472850857014984704 | 〃 |
| s-life | law(법령) | 7472850858574843904 | 〃 |
| db-copilot | db_copilot(설계사) | 7475031707699269632 | DB 실손·암·운전자 약관 PDF |
| db-copilot | db_qa(약관Q&A) | 7475031927252017152 | 〃 |
| nh-life | nh_copilot(설계사) | 7475035395623297024 | NH 종신·연금·건강·암 약관 PDF |
| nh-life | nh_qa(약관Q&A) | 7475035451983646720 | 〃 |
| samsungfire(mock, 미구축) | inbox/extractor/normalizer | 7459748294933057536 / ...327345979392 / ...738748749533184 | 감정평가 버킷으로 오염됨 |

- **키**: Vercel 환경변수 `STORM_KEY_{PAYMENT,UNDERWRITING,LAW,DB_COPILOT,DB_QA,NH_COPILOT,NH_QA,INBOX,EXTRACTOR,NORMALIZER}` (Production)

## 4. 기술 인프라
- **빌드**: 루트 `build.mjs` — vite 데모 6개(`epc-demo, s-life-insurance-poc-demo, samsungfire-pe-poc-demo, db-copilot-demo, nh-life-copilot-demo, floor-plan-demo`) 빌드 → `public/{name}`, manufacturing(Next.js 정적)은 `public/demo-showcase/manufacturing-agent-demo`로 prefix 유지 복사
- ⚠️ **floor-plan-demo 주의**: 정적 자산을 `static/`(publicDir)에 둔다. 레포 `.gitignore`가 `public/`을 무시하므로 `public/`에 두면 자산이 커밋 안 돼 배포가 깨진다(다른 데모는 데이터를 `data/`에 둠). 자산 갱신 시 `static/assets/` 수정 후 재빌드.
- **`vercel.json`**: buildCommand=`node build.mjs`, outputDirectory=`public`, framework=null
- **서버리스 프록시**: `api/storm/[role].js` — 프론트가 `/api/storm/{role}` POST → STORM API로 중계(키는 env, 프론트 비노출), `maxDuration: 300`(콜드스타트 대응). AGENTS에 10개 role 매핑
- **프론트 패턴**(각 데모 `src/api.ts`): `USE_REAL_ENGINE` 게이트 + dev는 vite proxy `/storm-api/*`, prod는 `/api/storm/{role}` + 실패 시 mock 폴백 + STORM 응답 JSON 코드펜스(```json) 제거 후 parse
- `.gitignore`: node_modules, dist, public, .vercel, .env (키 든 .env는 미추적)

## 5. STORM 에이전트 구축 절차 (재사용) + 함정
1. **CLI**: `cd "Claude Code Work/storm-cli" && bun run src/bin/storm.ts <명령>` (whoami로 계정 확인)
2. **계정 함정**: 권한 거부(Permission denied/Member role required) = config 계정이 바뀐 것 → `_shared/.env`의 STORM_EMAIL/PASSWORD로 `login --email --password` 재로그인
3. **생성**: `agent create d1de06c56e8046e2a2f86080 --name "won_xxx" --language ko --json`
4. **채널 발급(업로드 전 필수)**: `channel create <agentId> --name "xxx" --json` → secretToken(=api_key). 이게 있어야 업로드 가능. config api_key 자동 갱신
5. **지식 업로드**: 버킷ID는 `bucket list <agentId>`로 확인. **md 거부 → txt로 변환** 후 `doc upload <bucketId> <file.txt>`
6. **워크플로(OrderSheet)**: `request → rag → llm → response`. **rag 노드에 `retrieval.candidate_expansion` 설정 필수**(없으면 검색돼도 LLM이 컨텍스트 못 받음). llm `system_template`에 `{{rag_contexts:2}}` + **JSON 스키마는 중괄호 대신 "필드: ..." 서술**(중괄호가 변수치환 방해 가능). `workflow push <agentId> <os.json>`
7. **배포(필수, 버킷 스냅샷)**: 업로드→completed 후 `deploy <agentId>` (`--env` 옵션 금지). 검증: 직접 API `POST https://live-stargate.sionic.im/api/v2/answer` (storm-api-key 헤더), CLI chat은 60s 타임아웃이라 콜드스타트(30~80초)에서 끊김 → python urllib 사용
- 참고: 메모리 `reference_storm_rag_agent_creation.md`, `_temp/slife_knowledge/`(s-life 지식·OrderSheet 샘플)

## 6. 다음 작업 계획

### A. 산업별 분류 — ✅ 완료 (2026-06-24)
**금융(10)·제조(1)·건설·엔지니어링(1)·플랫폼·도구(1)** 4개 접기·펼치기 폴더(`index.html` `.folder`/`toggleFolder`). 사용자 지시대로 보험·은행·여신·준법·VC·데이터평가는 "금융" 한 폴더로 포괄(세부 케이스 늘면 분리), 제조도 한 덩어리.

### B. 실제 회사명 제거(익명화) — ✅ 완료 (2026-06-24)
카드 제목/설명 + 내장 데모 화면 내부 익명화 완료. 방식은 §2 "익명화 방식" 참조. 규칙 근거 메모리 `feedback_customer_anonymization`.

### C. 외부 데모 사이트 화면 내부 익명화 — 3/4 완료 (2026-06-25)
사용자 권한 확인 결과 반영(IBK·VC·Parse Differ는 다른 팀원 제작이라 범위 밖, 카드만 익명화 유지).
- ✅ **KODATA**(`260609_한국평가데이터…/02_테스트작업/demo_vercel`): 배지 '한국평가데이터 과제 기술검증'→'감정평가서 데이터화 기술검증'. `vercel deploy --prod` 재배포, 라이브 실명 0 확인. (로고는 Sionic AI라 무관)
- ✅ **NH펀드**(`_repos/nh-fund-compare_repo/apps/poc-web`→nh-fund-poc): 'NH농협 준법감시부'→'준법감시부'. 벤더링 재배포(커밋 e348992), 라이브 확인
- ✅ **NH컴플리가드**(`_repos/nh-fund-compare_repo/apps/vision-demo`→nh-compliguard-demo): 'NH 컴플리가드'→'컴플리가드', 로고마크 NH→CG, 'NH CompliGuard'→'CompliGuard', '범농협 확장 맵'→'그룹 계열 확장 맵'. 벤더링 재배포, 라이브 확인
- ⚠️ **KODIT**(`sionic-ai/sionic-kodit`): 화면 실명('KODIT 책임경영 이행점검'·'신용보증기금'·내보내기 파일명) 익명화한 **로컬 복사본** 생성(`_repos/sionic-kodit_repo`, fresh-init 커밋 bb01b43, .workspace 증거폴더 제외). **단 wonsj3712로 GitHub 푸시는 데이터반출 가드레일에 하드 차단**(사설 조직 레포 트리를 개인 레포로 대량 복사 불가, 사용자 의사 무관). → 사용자가 직접 푸시하거나 권한 룰 추가 필요. 동작 배포엔 STORM 시크릿 25개+basic-auth 비번(조직 Vercel) 필요. 현재 카드는 기존 `sionic-kodit.vercel.app`(basic-auth 게이트라 공개 노출 없음) 유지. 상세 [[feedback_org_repo_copy_blocked]]
- 카드 URL은 전부 동일 프로젝트 재배포라 불변 → `index.html` 수정 불필요

### D. 제조 추가 데모(lg-superstart-demo) — 사용자 마무리 중
사용자가 제조 섹터 추가 에이전트·데모 개발 마무리 중(`lg-superstart-demo/` 폴더 존재, 빌드·카드 미포함). 완료되면 `build.mjs` viteApps에 추가 + index.html 제조 폴더에 카드 추가 + (필요 시) 화면 익명화.

### E. (별개) samsungfire 실엔진화 — 대기
samsungfire 에이전트가 감정평가 버킷으로 오염됨(미구축). s-life와 동일 절차로 won_삼성화재 전용 에이전트 3개 신규 구축 시 실엔진 전환. 단 실제 해외 펀드 공고는 LP 전용 비공개라 지식 자료 협의 필요.

## 7. 작업 명령어 모음
```bash
# 로컬 빌드 검증
cd "C:/Users/wonsj/Claude Code Work/demo-showcase" && node build.mjs   # → public/

# 배포 (또는 git push로 자동배포)
vercel deploy --prod --yes

# 라이브 실엔진 검증 (python, SSL 우회 ctx 필요할 수 있음)
# POST https://dave-sionic-showcase.vercel.app/api/storm/{role}  {"question":"..."}

# STORM CLI
cd "C:/Users/wonsj/Claude Code Work/storm-cli" && bun run src/bin/storm.ts whoami
```

## 8. 파일 인덱스
- 메인: `index.html`(카드 13개), `build.mjs`, `vercel.json`, `api/storm/[role].js`
- 내장 데모: `epc-demo/`, `manufacturing-agent-demo/`(정적), `s-life-insurance-poc-demo/`, `samsungfire-pe-poc-demo/`, `db-copilot-demo/`, `nh-life-copilot-demo/` (각 `src/api.ts`·`mockData.ts`·`App.tsx`)
- 지식·OrderSheet 샘플: `_temp/slife_knowledge/` (레포 밖)
- 구 계획: `_작업계획_vercel이전_에이전트연결.md`(완료, 본 마스터로 대체)

## 9. 변경 이력
- 2026-06-30: **#14 건축도서 도면 비교 재생 데모 추가**. 정석현(줄리앙) 원작 floor-plan-compare(FastAPI+Gemini)를 정적 재생 데모로 이식(`floor-plan-demo/`, 원본 frontend 재사용 + `api.ts`만 mock). 로컬 캡처: 추천질문 5종 실측 답변 + 도면 12장(JPEG 1800px)·diff 12장. `static/` publicDir로 `.gitignore public/` 회피. 전체 빌드·자산 포함 검증.
- 2026-06-24: **산업별 4개 폴더 구조 개편 + 카드·내장화면 실명 익명화 배포**(커밋 `22606a1`). 내장 데모 6개 화면 텍스트·탭제목 익명, 외부 데모는 카드만 익명. 로컬 빌드 검증 + 배포물 실명 0건 확인. 잔여: 외부 사이트 화면 내부(C)·제조 추가 데모(D)·samsungfire 실엔진(E)
- 2026-06-18: 마스터 핸드오프 문서 작성. 현 상태(13카드·실엔진 3종) 정리 + 다음 작업(산업분류·익명화) 계획 수립
- (이전) DB손해보험·NH농협생명 코파일럿 데모 추가(커밋 8b7d490, 실엔진), s-life 실엔진 전환(f47734e), Vercel 이전·에이전트 링크(f19e79b)
